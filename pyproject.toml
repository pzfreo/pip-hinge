[project]
name = "pip-hinge"
version = "0.1.0"
description = "Parametric print-in-place piano hinge in build123d"
requires-python = ">=3.10,<3.14"
dependencies = [
    "build123d>=0.6",
]

[tool.uv]
package = false
